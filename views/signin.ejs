<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/x-icon" href="/images/eduPro_logo.jpg">
  <title>Log In</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css">
  <link rel="stylesheet" href="css/signin.css">
  <!-- font family -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link
    href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&family=Poppins:wght@200;300;400;600;700&display=swap"
    rel="stylesheet">
</head>

<body>
  <div class="container">
    <a href="/">
      <img id="close-button" src="Images\close-button.png" alt="Back" title="Back" width="25px">
    </a>
    <h1>Welcome back!</h1>
    <form id="login-form" action="/login" method="post">
      <form id="login-form" method="post">
        <div class="form-group">
          <label id="email-label" for="email">Email address</label>
          <input type="email" id="email" name="email" required autocomplete="email" placeholder="Enter your email">
        </div>
        <div class="form-group">
          <label id="password-label" for="password">Password</label>
          <div class="pswd-group">
            <input type="password" id="password" name="password"
              pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[\W_])(?=.*\S).{8,}"
              title="Password must contain at least one lowercase letter, one uppercase letter, one number, one special character, and be at least 8 characters long."
              placeholder="Enter your password" required>
            <i class="far fa-eye-slash" id="togglePassword"></i>
          </div>
        </div>
        <a id="forgot-pswd" href='#' onclick="openResetPasswordModal()">Forgot Password</a><br>
        <input id="login-btn" type="submit" value="Log in">
      </form>
      <!-- Modal popup -->
      <div id="reset-password-modal" class="modal">
        <!-- Modal content -->
        <div class="modal-content">
          <span class="close" onclick="closeResetPasswordModal()">&times;</span>
          <!-- Reset password form -->
          <h2>Reset Password</h2>
          <form id="reset-password-form" action="/reset-password" method="post">
            <label for="email">Email</label>
            <input type="email" id="reset-email" name="email" placeholder="Enter your email" required>
            <label for="new-password">New password</label>
            <div class="pswd-container">
              <input type="password" id="reset-password" name="new_password"
                pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[\W_])(?=.*\S).{8,}"
                title="Password must contain at least one lowercase letter, one uppercase letter, one number, one special character, and be at least 8 characters long."
                placeholder="Enter new password" required><br>
              <i class="far fa-eye-slash" id="toggleResetPassword"></i>
            </div>
            <label for="new-password">Confirm password</label>
            <div class="pswd-container">
              <input type="password" id="confirm-password" name="confirm_password"
                pattern="(?=.*\d)(?=.*[a-z])(?=.*[A-Z])(?=.*[\W_])(?=.*\S).{8,}"
                title="Password must contain at least one lowercase letter, one uppercase letter, one number, one special character, and be at least 8 characters long."
                placeholder="Confirm new password" required><br>
              <i class="far fa-eye-slash" id="toggleConfirmPassword"></i>
            </div>
            <button type="submit">Reset Password</button>
          </form>
        </div>
      </div>
    </form>
    <div class="sign-in">
      <p>Don't have an account? <a id="login" href="/signup">Sign Up</a></p>
    </div>
  </div>
  <div class="container"><img src="Images\login-ill.png"></div>
  
  <script src="/js/signin.js"></script>
</body>
</html>